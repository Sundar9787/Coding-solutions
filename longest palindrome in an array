arr=['racecar','level','hello','madam','world']
longest_palindrome=""
for word in arr:
    if word == word[::-1]:
        curr_length = len(word)
        if curr_length > len(longest_palindrome):
            longest_palindrome=""
            longest_palindrome= longest_palindrome + word
     
 
print(longest_palindrome)
